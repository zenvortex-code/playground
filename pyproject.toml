[project]
name = "pg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Oleg", email = "oleg.bestcennyi@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "playwright>=1.62.0",
    "pytest>=9.1.1",
    "pytest-html>=4.2.0",
    "pytest-playwright>=0.9.0",
]

[project.scripts]
pg = "pg:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
